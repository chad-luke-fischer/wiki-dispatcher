# Purpose: vault-map

Every traversal skill assumes an orientation exists. Without it, agents guess folder roles and miss the root MOC (the cheapest catalog there is). Pattern: [[patterns/missing-orientation]] (seed).
